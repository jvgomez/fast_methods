clear all; close all;

grad = load('../build/grad.txt');
% grad(:,end-2:end)=rad2deg(grad(:,end-2:end));
diff = abs(grad(:,10) - grad(:,11));
a=find(diff~=0);
c=find(diff==0);

diff2=grad(:,11)-grad(:,12);
d=find(diff2~=0);

[grad(d,([7, 11]))]



% idx=find(grad(:,end)<0);
% grad(idx,end)=grad(idx,end)+360;

% map=zeros(max(grad(:,1))+1, max(grad(:,2))+1);
% map(grad(:,1)+1, grad(:,2)+1)=sqrt(grad(:,3).^2+grad(:,4).^2);
% for i=1:length(grad(:,1))
%     map(grad(i,1), grad(i,2))=sqrt(grad(i,3).^2+grad(i,4).^2);
% end
% 
% max_n=max(max(map));
% map=map./max_n;
% figure
% axis equal
% % quiver(grad(:,1),grad(:,2),grad(:,3),grad(:,4))
% % hold on
% quiver(grad(:,1),grad(:,2),grad(:,5),grad(:,6), 'AutoScaleFactor', 10)

    